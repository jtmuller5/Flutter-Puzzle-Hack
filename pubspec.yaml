name: puzzle
description: Post about all your different interests
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  device_info_plus: ^3.2.1
  file_picker: ^4.4.0
  flutter_portal: ^0.4.0
  gap: ^2.0.0
  get_it: ^7.2.0
  get_it_mixin: ^3.1.4
  google_fonts: ^2.2.0
  go_router: ^3.0.2
  image: ^3.1.1
  injectable: ^1.5.3
  intl: ^0.17.0
  js: ^0.6.3
  rive: ^0.8.1
  simple_animations: ^4.0.1
  stacked: ^2.2.8
  transparent_image: ^2.0.0
  universal_io: ^2.0.4
  universal_html: ^2.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.1.7
  flutter_launcher_icons: ^0.9.2
  flutter_native_splash: ^1.3.2
  flutter_lints: ^1.0.4
  hive_generator: ^1.1.1
  injectable_generator: ^1.5.2
  json_annotation: ^4.4.0

# flutter pub run flutter_launcher_icons:main
flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/skylines_logo_small.png"
  remove_alpha_ios: true

# flutter pub run flutter_native_splash:create
#flutter_native_splash:
 # color: "#ffffff"
  #image: "assets/images/logo.png"

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/animations/
